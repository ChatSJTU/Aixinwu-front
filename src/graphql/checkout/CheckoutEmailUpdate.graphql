mutation CheckoutEmailUpdate($email: String = "", $id: ID = "") {
  checkoutEmailUpdate(email: $email, id: $id) {
    checkout {
      id
    }
    errors {
      code
      field
      message
      lines
    }
  }
}